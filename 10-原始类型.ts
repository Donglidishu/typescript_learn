let age1: number = 18; //整数、浮点、NaN(not a number,唯一一个不等于自身的数) 以及Infinity无穷
let myName: string = 'xuzhaokang';
let isLoading: boolean = true;
let a: null = null;
let b: undefined = undefined; //表示变量未初始化
let s: symbol = Symbol('id'); //用于生成全局唯一标识符
